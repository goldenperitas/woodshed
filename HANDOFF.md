# 引き継ぎ — Woodshed ローカルファースト化

**未解決のバグは無い**（`npm run test:offline` 全項目 PASS）。

直近の作業は個別のバグ潰しではなく、**不安定さの根を断つ**もの。§2 を先に読むと
「なぜこの構成なのか」が最短で分かる。

まず `README.md` の「データの流れ」を読むこと。ここではそこに書いていない
「今どうなっているか」「何を壊してはいけないか」だけを書く。

---

## 1. いま何が起きたのか

サーバーレンダリングのアプリを、**端末内 SQLite (OPFS/wasm) から描画する
ローカルファースト構成**に全面移行した。Mac は `/api/sync` の同期ピアに降格。
目的は2つ:

- 圏外でも全機能が動く
- 音源をサーバーに置かない（他人に使わせる場合の法的要件。詳細は §5）

関連コミット（新しい順）:

| コミット | 内容 |
| --- | --- |
| `764350f` | オフラインテストハーネス、診断画面への導線、SWのシェル保存条件を修正 |
| `7664ee7` | 検証できなかったSW変更を撤回、`/debug` 追加 |
| `aa07b5e` | 曲IDが遷移に追従しない問題を修正 |
| `36b80b1` | 一覧が0件になる問題、ジャケ写が出ない問題を修正 |
| `2c02e45` | ローカルファースト化本体 |
| `7c6866d` | 圏外で別の曲を押すと前の曲に着地する問題を修正 |
| `483690a` | 端末で起きたことを記録する計器 |
| `a4f1ae2` | オフラインテストを3つの状況に分割 |
| `cc7e6e2` | **画面の切り替えを端末側で行うようにした（根本策）** |
| `517cd70` | 緩衝材の撤去 |

---

## 2. 不安定さの根と、その断ち方

### 根はひとつだった

オフラインでは Next のルーターがルートのペイロードを取りに行けない。結果、
**どの遷移もフルページ読み込みに落ちる**。1タップごとに文書・Worker・SQLite
接続が作り直され、出ていく文書と入ってくる文書が同じ OPFS の排他ハンドルを
奪い合う。§3 の7件のうち5件はここから生えていた。

計器で測った実際の数字（11タップのオフライン一巡）:

| | 変更前 | 変更後 |
| --- | ---: | ---: |
| 文書ロード | 11 | **0** |
| DB開き直し | 11 | **0** |
| payload取り逃し | 35 | **0** |

### いまの構成

- **全ルートが同じ `<Screen>` を描く**（`components/Screen.tsx`）。Screen が
  `usePathname()` で画面を選ぶ。`app/**/page.tsx` は全部 `export { default }`
  の1行で、コールドロードとリンク共有のためだけに存在する
- **遷移は `history.pushState`**（`lib/nav.ts` / `components/ViewLink.tsx`）。
  Next の shallow routing に乗っている。ルーターに何も尋ねないので圏外でも
  取りに行くものが無く、**文書は生き続け、DBは1回開いたら開いたまま**
- **画面の選択をサーバー描画では行わない**。`useSyncExternalStore` で
  ハイドレート後に決める。これで文書がどのURLでもバイト単位で同じになる
- **SWのシェルは1枚**（`/`）。どのURLへのナビゲーションもこれ1枚が答える

3番目と4番目が効いている点が大事。「あるURL向けに取った文書が、別のURLの
文書として渡る」形が構造ごと消えた。**これがかつて2つのバグを生んだ形**
（下の「別の曲に着地」と「生RSCペイロード表示」）。

### 直近で直したバグ — 「別の曲を押すと前の曲に着地する」

原因は、SWが**ルーターのペイロード要求（`RSC: 1`）にHTMLシェルを返していた**
こと。シェルの実体は最後にオンラインで開いた曲のHTMLで、その Response は
`Response.url` にその曲のURLを持つ。Next のルーターは返答がペイロードでないと
判断するとフルページ遷移に切り替え、**遷移先として `response.url` を使う**。

引き継ぎ資料にあった仮説「ナビゲーションに別URLの Response を返すとブラウザが
文書URLを合わせる」は**誤り**だった。URLを剥がす修正案だけでは直らない。
SWにログを仕込んで実際の並びを確認してから修正している。

いまはペイロード要求自体がほぼ発生しない（ルーターを使っていないので）。
**`sw.payload.miss` が0より大きくなったら、どこかがルーター遷移に戻った合図。**

---

## 3. 過去に直したもの（再発させないこと）

§2 の変更で**根ごと消えた**ものには ✓ を付けた。残りは今も対処が要る。

| | 症状 | 原因 | 対処 |
| --- | --- | --- | --- |
| ✓ | 別の曲を押すと前の曲に着地 | SWがRSCペイロード要求にHTMLシェルを返し、ルーターがその `response.url` へフルページ遷移した | ペイロード要求は本物のペイロードがある時だけ返し、無ければ落とす（`public/sw.js`） |
| ✓ | 一覧が0曲になる | 圏外の遷移がフルページ読み込みに落ち、前の文書のWorkerがOPFSハンドルを掴んだまま次が起動 | `pagehide` でプールを明け渡し、次のクエリで開き直す（`public/db-worker.js`） |
| ✓ | 曲→曲で前の曲が出る | ページが再マウントされず、URLからIDを読むeffectが再実行されない。`pushState` は `popstate` を発火しない | `usePathname()` を依存に追加（`lib/local/store.ts`） |
| ✓ | 一瞬「見つかりません」 | ID確定前の `null` を結果として描画 | クエリ結果に入力キーを持たせる（`lib/local/store.ts`） |
|   | ジャケ写が消える | SWのバージョンを上げるとキャッシュ全消し | ジャケットを `woodshed-media` に分離（`public/sw.js`） |
| ✓ | 曲ページに生のRSCペイロードが表示 | Nextの先読みペイロードをシェルとして保存 | `isDocumentResponse()` でHTML文書のみ保存 |
|   | 退避されると行き止まり | 空のDBを検知して例外を投げるだけだった | オンラインなら黙って再同期し、後から知らせる。復旧できない時だけ止める（`lib/local/db.ts`） |
|   | 別タブがあるとアプリが開けない | Web Lock を必須条件にしていた。bfcacheで凍結された文書がロックを保持したままになりうる | ロックは助言的な待ち合わせに変更。可否はアクセスハンドルで判定 |

---

## 4. 壊してはいけない不変条件

- **`next/link` と `useRouter` を使わない。** ルーターにペイロードを要求した
  瞬間、圏外の遷移がフルページ読み込みに戻る。アプリ内リンクは `ViewLink`、
  プログラム遷移は `navigate()`（`lib/nav.ts`）
- **`<Screen>` の画面選択をサーバー描画に持ち込まない。** どのURLでも同じ
  文書が返ることが、1枚のシェルで全画面をまかなえる根拠になっている
- **音源・ジャケットの原本はMacに置く。** 端末が持つのは複製。ここを崩すと
  「端末のストレージが退避されたら音源は戻らない」に逆戻りする。**ただし
  これは「他人には使わせない」という前提の上でのみ成立する**（§5）
- **物理削除しない。** 必ず `deleted_at` のトゥームストーン。`DELETE` すると
  同期で復活する
- **IDはUUID。** 連番に戻すとオフライン採番が衝突する
- **プル用カーソルは `server_seq`（サーバー採番）。** 端末時計の `updated_at` を
  使うと時計ズレで行を取りこぼす
- **OPFSは SAHPool VFS 固定。** 既定の `opfs` VFS や SQLocal は COOP/COEP
  ヘッダを要求する。SAHPoolだけが不要（実測確認済み）
- **DB接続はモジュールスコープのシングルトン。** effect内でWorkerを作ると
  StrictModeの二重マウントで壊れる（`lib/local/db.ts`）
- **`assertNotSilentlyEmpty()` を外さない。** 空のDBを「0件の棚」として
  描画しないための安全弁。トゥームストーンで総行数は単調増加するので
  「前はあったのに0」は異常と断定できる。1文書につき1回だけ走る

---

## 5. 法的な前提（設計制約）

**この構成は「使うのは持ち主だけ」という前提の上に立っている。** 前提が変わる
なら、音源の置き場所から見直すこと。

日本では MYUTA事件（東京地判 平19.5.25）で、ユーザーが自分の音源をアップロード
し自分だけがダウンロードするサービスについて、**運営者が複製・送信可能化の
主体**と認定されている。ロクラクII（最判 平23.1.20）・まねきTV（最判 平23.1.18）
も同方向。**アカウントごとにデータを分離しても防御にならない。**

この論点は「運営者が他人にサービスを提供する」場面のもので、自分のMacに自分の
音源を置いて自分の端末から聴くだけなら当たらない。だから現在の形（原本はMac、
端末は複製）で問題ない。

**他人に使わせる話が出たら、ここが最初に効く。** その時は
音源をサーバーから外すか、少なくとも法的整理をやり直すこと。認証を足すだけでは
足りない（アカウント分離は防御にならない、が上記の判断）。

### 経緯

ローカルファースト化の際、音源を端末のみ（`local:<uuid>`）に置く形に一度変えた。
その後、**端末のストレージが iOS に退避されたら音源が戻らない**という耐久性の
問題が明確になり、ユーザー確認のうえ「原本はMac、端末は複製」に戻している
（`app/api/media/route.ts`）。実データは20件とも一貫してMac側にあり、
`local:` の行は結局1件も作られていない。

## 6. 開発環境の重要な制約と、実機の見方

### エージェントのブラウザでは Service Worker を登録できない

`mcp__Claude_Browser__*` のブラウザは、**空の1行 SW ですら登録に失敗する**:

```
TypeError: Failed to register a ServiceWorker ...
An unknown error occurred when fetching the script.
```

`scope` や `type` を変えても同じ。`navigator.serviceWorker` は存在し、
`isSecureContext` も true、`/sw.js` の fetch も 200。ブラウザ側の制約。
`mcp__claude-in-chrome__*`（実Chrome）も未接続で使えなかった。

**だから `scripts/offline-test.js` がある。** 4つの状況（一巡・二重・開き直し・
退避）をそれぞれ独立したブラウザコンテキストで走らせる。SW に手を入れたら必ずこれで
確認すること。前任者はこれを作る前に推測でSWを変更し、実機のナビゲーションを
壊した（`7664ee7` で撤回）。**検証できない変更を「修正」として出さないこと。**

なお SW 以外（DB・同期・画面遷移・クエリ）はエージェントのブラウザで
問題なく検証できる。

### 実機の中を見る手段は、端末の記録しかない

`lib/local/log.ts` が localStorage のリングバッファに、この端末で起きたことを
書いている。`/debug` の「できごと」で読めて、コピーもできる。

記録するもの: 文書の起動（種別・パス・PWAか・SW制御下か）、DBを開いた実コスト
（ms・OPFSハンドルの取得試行回数・ロック待ち）、DBの明け渡し、同期の成否、
SWがオフラインで何を返したか、投げられた例外。Worker には localStorage が
無いので、DB側の診断は init の返信に乗せて page 側で書いている。

`/debug` の先頭の計器で見るべきは**「文書ロード数」と「DB開き直し数」の組**。
オフラインで画面を切り替えても、ここが増えないのが正常。増えていたら §2 の
根が生え直している。

### アサーションを緩くしない

初版のテストは、**画面にRSCペイロードが生表示されている状態を PASS と
報告した**。「エラーが出ていない」は「動いている」ではない。
`scripts/offline-test.js` の `tuneState()` が何を弾いているか見てから緩めること。

---

## 7. 実機での確認手順

SWのバージョンを上げたら（`public/sw.js` の `CACHE`）、必ず:

1. **オンラインで** PWA を開く
2. アプリスイッチャーから**落とす**
3. 開き直す
4. 一覧最下部の「診断」→ `SW 状態: activated` / `SW 制御下: yes` を確認
5. オンラインのまま各画面を一巡（一覧は下までスクロール）。診断で4つの
   アセットが全部「あり」になれば準備完了
6. 診断で「記録を消す」
7. 機内モード ON → アプリを落とす → 起動
8. 圏外で普通に一巡し、診断に戻る。**画面を切り替えたぶんだけ「文書ロード」が
   増えていたら、遷移がフルページ読み込みに戻っている。** 起動の1回以外は
   増えないのが正しい

`/debug` には再同期と「DBを作り直す」がある。作り直しは安全（メタデータは
Macから、音源はIndexedDBに残る）。

---

## 8. 残タスク

1. `data/woodshed-pre-uuid-*.db` は移行前のバックアップ。しばらく残す
2. 実機で一度も検証していないもの: 音源の追加（Macから）と、退避からの復旧。
   どちらも Mac 上の Chrome では通っている

認証は不要と確認済み（他人には使わせない）。

## 9. 状態メモ

- 実データ移行済み: 81曲 / 音源20件 / 復習ログ1件（`scripts/migrate-to-uuid.js`、再実行可）
- `scripts/import-standards.js` はユーザーの未追跡ファイル。コミットしないこと
- ポート3000でユーザーが `next start` を動かしている可能性がある。
  検証には3100を使う
